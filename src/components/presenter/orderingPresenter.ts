import { IOrdering, IOrderingData } from "../../types";
import { IEvents } from "../base/events";

export class OrderingPresenter{
  private _ordering: IOrdering;
  private _orderingData: IOrderingData;

  constructor(events: IEvents, ordering: IOrdering, orderingData: IOrderingData, ) {

  }
}